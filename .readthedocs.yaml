# Read the Docs configuration (optional: the book can also be hosted on GitHub Pages)
version: 2
build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  jobs:
    pre_build:
      - "jupyter-book config sphinx ."
sphinx:
  configuration: conf.py
python:
  install:
    - requirements: requirements.txt
